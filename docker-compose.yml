version: '3'

services:

  web:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./app:/var/www/apache-flask/app
      - ./data:/var/www/apache-flask/data
    restart: always
    container_name: web
